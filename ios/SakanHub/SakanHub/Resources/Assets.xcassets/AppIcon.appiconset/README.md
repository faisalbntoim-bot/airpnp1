# App Icon

Drop the final `AppIcon-1024.png` (1024×1024, no alpha channel, no transparency)
into this folder before Archive. iOS 14+ derives every smaller size from
the 1024 master.

Do not commit a placeholder icon into a distribution build — the App Store
rejects the default Xcode icon.
