# SakanHubb web — public marketing site.
#
# ABSOLUTE RULE: this project must NEVER carry a secret. No JWT signing key,
# no payment provider key, no database credential, no admin token. Public
# marketing pages only. Server components run on Vercel edge / node — treat
# them as public.
#
# The only variables allowed here are PUBLIC configuration.

# The public marketing site URL, used for canonical links + OG images.
NEXT_PUBLIC_SITE_URL=https://sakanhub.example
